{
  "name": "search_code",
  "description": "Search the repository for code matching a given pattern. Returns file paths and matching line numbers.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search pattern (supports regex)."
      },
      "path": {
        "type": "string",
        "description": "Optional directory path to restrict the search."
      }
    },
    "required": ["query"]
  }
}
